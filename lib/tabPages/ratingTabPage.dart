import 'package:flutter/material.dart';

class RatingTabPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("RatingTabPage"),),
    );
  }
}